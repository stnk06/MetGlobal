<UserControl x:Class="MetGlobal.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/SharedResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Button" BasedOn="{StaticResource DefaultButton}">
                <Setter Property="Margin" Value="5,0"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="15,6"/>
            </Style>

            <!-- АДАПТАЦИЯ ТАБЛИЦ ПОД БЕЛЫЙ ФОН -->
            <Style TargetType="DataGrid" BasedOn="{StaticResource ModernDataGrid}">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <!-- Основные цвета для светлой темы -->
                <Setter Property="RowBackground" Value="White"/>
                <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
                <Setter Property="Foreground" Value="#333333"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="#EEEEEE"/>

                <!-- Заголовки колонок (темный текст) -->
                <Setter Property="ColumnHeaderStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridColumnHeader}">
                            <Setter Property="Foreground" Value="#444444"/>
                            <Setter Property="BorderBrush" Value="#CCCCCC"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                    </Setter.Value>
                </Setter>

                <!-- Строки (светлый фон + эффект наведения) -->
                <Setter Property="RowStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridRow" BasedOn="{StaticResource ModernDataGridRow}">
                            <Setter Property="Background" Value="White"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#F0F0F0"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BrushRed}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>

                <!-- Ячейки -->
                <Setter Property="CellStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridCell" BasedOn="{StaticResource ModernDataGridCell}">
                            <Setter Property="Foreground" Value="Black"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <DockPanel Grid.Row="0" Margin="0,0,0,20">
            <Label Content="Аналитика и Отчетность" DockPanel.Dock="Left" VerticalAlignment="Center" Margin="0"/>
            <Button Content="Обновить данные" Command="{Binding RefreshCommand}" DockPanel.Dock="Right" Width="117" Height="36" HorizontalAlignment="Right" />
        </DockPanel>

        <!-- Вкладки -->
        <TabControl Grid.Row="1" mah:TabControlHelper.Underlined="TabPanel" Background="Transparent">

            <!-- ВКЛАДКА 1: КАТЕГОРИИ -->
            <TabItem Header="Категории" Style="{DynamicResource MahApps.Styles.TabItem}">
                <!-- Белая карточка -->
                <Border Background="White" CornerRadius="10" Margin="0,15,0,0" Padding="20" TextElement.Foreground="Black">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Тулбар вкладки -->
                        <DockPanel LastChildFill="False" Margin="0,0,0,20">
                            <TextBlock Text="Распределение выручки по категориям" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#333"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <Button Content="PDF" Command="{Binding ExportCategoryPdfCommand}" CommandParameter="{Binding ElementName=CategoryChart}" Height="35" Width="85"/>
                                <Button Content="Excel" Command="{Binding ExportCategoryExcelCommand}" Height="35" Width="85"/>
                            </StackPanel>
                        </DockPanel>

                        <Grid Grid.Row="1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="1.5*"/>
                            </Grid.ColumnDefinitions>

                            <!-- График (без черной подложки) -->
                            <lvc:PieChart x:Name="CategoryChart" Series="{Binding CategorySeries}" LegendLocation="Bottom" Hoverable="True" InnerRadius="40"/>

                            <!-- Таблица -->
                            <DataGrid Grid.Column="2" ItemsSource="{Binding CategoryReports}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Категория" Binding="{Binding CategoryName}" Width="*"/>
                                    <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=C, ConverterCulture=ru-RU}" Width="Auto"/>
                                    <DataGridTextColumn Header="Доля" Binding="{Binding Percentage, StringFormat=P1}" Width="Auto"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ВКЛАДКА 2: ДИНАМИКА -->
            <TabItem Header="Динамика продаж" Style="{DynamicResource MahApps.Styles.TabItem}">
                <!-- Белая карточка -->
                <Border Background="White" CornerRadius="10" Margin="0,15,0,0" Padding="20" TextElement.Foreground="Black">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel LastChildFill="False" Margin="0,0,0,20">
                            <TextBlock Text="Статистика продаж по месяцам" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#333"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <Button Content="PDF" Command="{Binding ExportMonthlyPdfCommand}" CommandParameter="{Binding ElementName=MonthlyChart}" Height="35" Width="85"/>
                                <Button Content="Excel" Command="{Binding ExportMonthlyExcelCommand}" Height="35" Width="85"/>
                            </StackPanel>
                        </DockPanel>

                        <Grid Grid.Row="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="2*"/>
                                <RowDefinition Height="30"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- График -->
                            <lvc:CartesianChart x:Name="MonthlyChart" Series="{Binding MonthlySeries}" LegendLocation="Top">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Labels="{Binding MonthlyLabels}" Foreground="Gray" FontSize="12"/>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis LabelFormatter="{Binding YFormatter}" Foreground="Gray" FontSize="12"/>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>

                            <DataGrid Grid.Row="2" ItemsSource="{Binding MonthlyReports}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Месяц" Binding="{Binding Month}" Width="*"/>
                                    <DataGridTextColumn Header="Кол-во заказов" Binding="{Binding OrdersCount}" Width="Auto"/>
                                    <DataGridTextColumn Header="Сумма продаж" Binding="{Binding TotalAmount, StringFormat=C, ConverterCulture=ru-RU}" Width="Auto"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ВКЛАДКА 3: РЕЙТИНГ КЛИЕНТОВ -->
            <TabItem Header="Клиенты" Style="{DynamicResource MahApps.Styles.TabItem}">
                <!-- Белая карточка -->
                <Border Background="White" CornerRadius="10" Margin="0,15,0,0" Padding="20" TextElement.Foreground="Black">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel LastChildFill="False" Margin="0,0,0,20">
                            <TextBlock Text="Топ покупателей по объему" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#333"/>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                                <Button Content="PDF" Command="{Binding ExportCustomerPdfCommand}" Height="35" Width="85"/>
                                <Button Content="Excel" Command="{Binding ExportCustomerExcelCommand}" Height="35" Width="85"/>
                            </StackPanel>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding CustomerReports}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Компания" Binding="{Binding CompanyName}" Width="2*"/>
                                <DataGridTextColumn Header="Заказов" Binding="{Binding OrdersCount}" Width="*"/>
                                <DataGridTextColumn Header="Сумма покупок" Binding="{Binding TotalAmount, StringFormat=C, ConverterCulture=ru-RU}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ВКЛАДКА 4: ПРОДУКЦИЯ ПО ПОСТАВЩИКАМ -->
            <TabItem Header="Поставщики" Style="{DynamicResource MahApps.Styles.TabItem}">
                <!-- Белая карточка -->
                <Border Background="White" CornerRadius="10" Margin="0,15,0,0" Padding="20" TextElement.Foreground="Black">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel LastChildFill="False" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Реализация продукции по поставщикам" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Сгруппировано по наименованию поставщика" FontSize="12" Foreground="Gray" FontStyle="Italic" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top">
                                <Button Content="PDF" Command="{Binding ExportSupplierPdfCommand}" Height="35" Width="85"/>
                                <Button Content="Excel" Command="{Binding ExportSupplierExcelCommand}" Height="35" Width="85"/>
                            </StackPanel>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding SupplierReports}">
                            <DataGrid.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <!-- Заголовок группы остается темным для контраста -->
                                            <Border Background="{StaticResource BrushMediumGray}" CornerRadius="4" Margin="0,5,0,0" Padding="10,5">
                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="White"/>
                                            </Border>
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </DataGrid.GroupStyle>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Поставщик" Binding="{Binding SupplierName}" Width="1.5*"/>
                                <DataGridTextColumn Header="Продукт" Binding="{Binding ProductName}" Width="2*"/>
                                <DataGridTextColumn Header="Артикул" Binding="{Binding ProductCode}" Width="*"/>
                                <DataGridTextColumn Header="Продано (шт)" Binding="{Binding TotalQuantitySold, StringFormat=N2}" Width="*"/>
                                <DataGridTextColumn Header="Выручка" Binding="{Binding TotalRevenue, StringFormat=C, ConverterCulture=ru-RU}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

            <!-- ВКЛАДКА 5: ЕЖЕДНЕВНЫЙ ОТЧЕТ -->
            <TabItem Header="Журнал операций" Style="{DynamicResource MahApps.Styles.TabItem}">
                <!-- Белая карточка -->
                <Border Background="White" CornerRadius="10" Margin="0,15,0,0" Padding="20" TextElement.Foreground="Black">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <DockPanel LastChildFill="False" Margin="0,0,0,20">
                            <StackPanel>
                                <TextBlock Text="Детальный журнал продаж по дням" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" Foreground="#333"/>
                                <TextBlock Text="Хронология заказов" FontSize="12" Foreground="Gray" FontStyle="Italic" Margin="0,5,0,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" VerticalAlignment="Top">
                                <Button Content="PDF" Command="{Binding ExportDailyPdfCommand}" Height="35" Width="85"/>
                                <Button Content="Excel" Command="{Binding ExportDailyExcelCommand}" Height="35" Width="85"/>
                            </StackPanel>
                        </DockPanel>

                        <DataGrid Grid.Row="1" ItemsSource="{Binding DailyReports}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Дата" Binding="{Binding OrderDateFormatted}" Width="1.5*"/>
                                <DataGridTextColumn Header="№" Binding="{Binding OrderID}" Width="0.5*"/>
                                <DataGridTextColumn Header="Клиент" Binding="{Binding CustomerName}" Width="2*"/>
                                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="*"/>
                                <DataGridTextColumn Header="Поз." Binding="{Binding ProductsCount}" Width="0.5*"/>
                                <DataGridTextColumn Header="Сумма заказа" Binding="{Binding OrderTotal, StringFormat=C, ConverterCulture=ru-RU}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>