<Window x:Class="MetGlobal.Views.EditSupplierWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="Карточка поставщика" Width="600" Height="450"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/SharedResources.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Border Padding="25" Background="{DynamicResource MahApps.Brushes.ThemeBackground}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Заголовок -->
                <RowDefinition Height="*"/>
                <!-- Форма -->
                <RowDefinition Height="Auto"/>
                <!-- Кнопки -->
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <TextBlock Text="Редактирование поставщика" 
                       Grid.Row="0"
                       FontSize="24" FontWeight="Light" 
                       Foreground="{DynamicResource MahApps.Brushes.Accent}" 
                       Margin="0,0,0,25"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="30"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- ЛЕВАЯ КОЛОНКА -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="ОСНОВНЫЕ ДАННЫЕ" FontWeight="Bold" Foreground="{StaticResource BrushLightGray}" Margin="0,0,0,10"/>

                    <!-- Название (Тип + Имя) -->
                    <Label Content="Название поставщика *" FontWeight="SemiBold"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="80"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="TypeComboBox" 
                                  Style="{DynamicResource MahApps.Styles.ComboBox}"
                                  mah:TextBoxHelper.Watermark="Тип"/>

                        <TextBox x:Name="NameTextBox" Grid.Column="1" Margin="5,0,0,0"
                                 Style="{DynamicResource MahApps.Styles.TextBox}"
                                 mah:TextBoxHelper.Watermark="Название"
                                 PreviewTextInput="BlockLatinInput"/>
                    </Grid>

                    <!-- Страна (Выпадающий список) -->
                    <Label Content="Страна *" FontWeight="SemiBold"/>
                    <ComboBox x:Name="CountryComboBox"
                              Style="{DynamicResource MahApps.Styles.ComboBox}"
                              SelectedValue="{Binding Country}"
                              mah:TextBoxHelper.Watermark="Выберите страну"
                              Margin="0,0,0,15"/>

                    <!-- ИНН (макс 10 цифр) -->
                    <Label Content="ИНН" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding INN, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{DynamicResource MahApps.Styles.TextBox}"
                             mah:TextBoxHelper.Watermark="10 цифр"
                             MaxLength="10"
                             PreviewTextInput="BlockNonDigits"
                             Margin="0,0,0,15"/>
                </StackPanel>

                <!-- ПРАВАЯ КОЛОНКА -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="КОНТАКТЫ" FontWeight="Bold" Foreground="{StaticResource BrushLightGray}" Margin="0,0,0,10"/>

                    <!-- Телефон (с выбором маски) -->
                    <Label Content="Телефон" FontWeight="SemiBold"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ComboBox x:Name="PhoneMaskComboBox" Grid.Row="0" Margin="0,0,0,5"
                                  Style="{DynamicResource MahApps.Styles.ComboBox}"
                                  FontSize="12"
                                  SelectionChanged="PhoneMaskComboBox_SelectionChanged"/>

                        <TextBox x:Name="PhoneTextBox" Grid.Row="1"
                                 Text="{Binding ContactPhone, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{DynamicResource MahApps.Styles.TextBox}"
                                 mah:TextBoxHelper.Watermark="+7 ..."
                                 PreviewTextInput="BlockNonDigits"
                                 TextChanged="PhoneTextBox_TextChanged"/>
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Сохранить" IsDefault="True" Click="SaveButton_Click" 
                         Width="120" Height="35"/>
                <Button Content="Отмена" IsCancel="True"
                         Width="100" Height="35" Margin="15,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>