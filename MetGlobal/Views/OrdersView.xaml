<UserControl x:Class="MetGlobal.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Управление заказами" Style="{DynamicResource MahApps.Styles.TextBlock.Title}" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <ComboBox mah:TextBoxHelper.Watermark="Статус"
                      ItemsSource="{Binding Statuses}" 
                      SelectedItem="{Binding SelectedStatus}"
                      Width="150" Margin="0,0,10,0" Style="{DynamicResource MahApps.Styles.ComboBox}"/>
            <DatePicker mah:TextBoxHelper.Watermark="Дата с"
                        SelectedDate="{Binding StartDate}" Width="140" Margin="0,0,5,0" Style="{DynamicResource MahApps.Styles.DatePicker}"/>
            <DatePicker mah:TextBoxHelper.Watermark="Дата по"
                        SelectedDate="{Binding EndDate}" Width="140" Margin="0,0,10,0" Style="{DynamicResource MahApps.Styles.DatePicker}"/>
            <Button Content="Применить" Command="{Binding SearchCommand}" Margin="0,0,5,0"/>
            <Button Content="Сбросить" Command="{Binding ResetFiltersCommand}"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" 
                      Style="{DynamicResource MahApps.Styles.DataGrid.Azure}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Номер" Binding="{Binding OrderID}" Width="Auto"/>
                    <DataGridTextColumn Header="Дата" Binding="{Binding OrderDate, StringFormat='dd.MM.yyyy HH:mm'}" Width="Auto"/>
                    <DataGridTextColumn Header="Клиент" Binding="{Binding CompanyName}" Width="*"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="Auto"/>
                    <DataGridTextColumn Header="Итоговая сумма" Binding="{Binding TotalAmount, StringFormat=C, ConverterCulture=ru-RU}" Width="Auto"/>
                </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent"/>

            <DataGrid Grid.Column="2" ItemsSource="{Binding SelectedOrderDetails}"
                      Style="{DynamicResource MahApps.Styles.DataGrid.Azure}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="2*"/>
                    <DataGridTextColumn Header="Количество" Binding="{Binding Quantity}" Width="*"/>
                    <DataGridTextColumn Header="Цена за ед." Binding="{Binding UnitPrice, StringFormat=C, ConverterCulture=ru-RU}" Width="*"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat=C, ConverterCulture=ru-RU}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <Grid Grid.Row="3" Margin="0,15,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" >
                <Button Content="Новый заказ" Command="{Binding AddOrderCommand}"  Margin="0,0,5,0"/>
                <Button Content="Изменить заказ" Command="{Binding EditOrderCommand}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Экспорт в PDF" Command="{Binding ExportToPdfCommand}" Margin="0,0,5,0"/>
                <Button Content="Экспорт в Excel" Command="{Binding ExportToExcelCommand}"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>