<UserControl x:Class="MetGlobal.Views.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Справочник: Поставщики" Style="{DynamicResource MahApps.Styles.TextBlock.Title}" Margin="0,0,0,15"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Suppliers}" SelectedItem="{Binding SelectedSupplier}"
                  Style="{DynamicResource MahApps.Styles.DataGrid.Azure}" CanUserAddRows="False" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Название поставщика" Binding="{Binding SupplierName}" Width="2*"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="*"/>
                <DataGridTextColumn Header="ИНН" Binding="{Binding INN}" Width="*"/>
                <DataGridTextColumn Header="Контактный телефон" Binding="{Binding ContactPhone}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,15,0,0">
            <Button Content="Добавить" Command="{Binding AddCommand}"  Margin="0,0,5,0"/>
            <Button Content="Изменить" Command="{Binding EditCommand}" Margin="0,0,5,0"/>
            <Button Content="Удалить" Command="{Binding DeleteCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>